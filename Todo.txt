Game -------------------------------------
A lot of the game still happens clientside,
which should be moved to the game server.

Catch CS_WeaponFired
-> store it as Games aimingData interface in the game,
-> then send SC_ExplosionOccured


- Server gets aiming data, then stores that to send a projectile
Such as : 
Also serve
choosing a weapon
Aiming a weapon
	Do we send the data every tick, or just every tick that the angle changes, or do we only send it once its locked in?